# dotfiles

Used for storing configuration files on the workstation. These `.<application-name>`-files (i.e. called dotfiles) are written back to git to have settings persist over a re-install of the workstation.

```shell
git clone https://github.com/levivannoort/dotfiles ~/dotfiles
```

```shell
cd dotfiles
```
